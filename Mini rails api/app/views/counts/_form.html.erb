<%= form_for(@count) do |f| %>
  <% if @count.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@count.errors.count, "error") %> prohibited this count from being saved:</h2>

      <ul>
      <% @count.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :action %><br>
    <%= f.number_field :action %>
  </div>
  <div class="field">
    <%= f.label :count %><br>
    <%= f.number_field :count %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
